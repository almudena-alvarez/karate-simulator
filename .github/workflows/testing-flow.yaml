name: Testing Flow

on:
  push:
    branches:
      - main
  workflow_dispatch:
  
jobs:
  call:
    uses: almudena-alvarez/composite-workflow-simulator/.github/workflows/reusable-workflow-testing.yaml@composite-branch
    secrets:
      FIRST_SECRET: ${{ secrets.FIRST_SECRET }}
